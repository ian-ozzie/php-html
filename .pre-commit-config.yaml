repos:
  - repo: local
    hooks:
      - id: lint
        name: PHPCS lint
        entry: composer run lint
        language: system
        types: [php]

      - id: test
        name: PHPUnit test
        entry: composer run test
        language: system
        types: [php]
        always_run: true
        pass_filenames: false
